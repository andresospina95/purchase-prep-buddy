services:
  cigo-oc:
    build: .
    image: cigo-oc:latest
    container_name: cigo-oc
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: "3000"
